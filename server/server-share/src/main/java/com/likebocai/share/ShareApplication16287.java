package com.likebocai.share;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication(scanBasePackages = {"com.likebocai.share"})
public class ShareApplication16287 {
    public static void main(String[] args) {
        SpringApplication.run(ShareApplication16287.class, args);
    }
}